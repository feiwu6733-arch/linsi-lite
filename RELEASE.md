# v0.6.0 · GitHub 版本更新

启动后自动检查 GitHub 正式版本，设置页查看更新内容与下载进度，支持稍后提醒和忽略版本。用户点击安装后检查所有工作台的运行任务，校验更新包，备份发行文件和数据库，再更新、重启；新版启动失败时恢复旧版。账号、作品、登录资料和 API 配置保留在本机。

GitHub Actions 在版本号更新后自动测试、打包和发布 Release。维护方法见 [版本发布与更新](docs/版本发布与更新.md)。

## v0.5.1 · AI API 配置版

研究重试修订：报告页的开始、重试和停止只处理当前报告，立即显示排队状态；通过独立批次入口恢复其余作品。手动重试优先于整批后续作品，已完成报告复用。停止后拒绝迟到响应写入，当前请求结束前不会重发同一报告。流式响应兼容保活、空数据、多行事件与 UTF-8 BOM，解析错误直接指向本次响应，不再一概提示更换接口。

修复领域异常被通用格式错误覆盖：取消、超时、读取上限、截断等原因各自保留。提高流式传输的字节上限以容纳事件元数据和思考输出，仍限制单事件和最终正文大小。DeepSeek 官方接口默认快速模式，可在设置中切换深度思考；其他接口不增加专属参数。

报告阅读补齐多级标题、列表、来源链接、表格与代码块；原始 HTML 和不安全链接协议不会执行。研究目录包含全部标题级别。

接口设置修订：按地址识别服务商，模型列表提供可见下拉选择；获取列表或测试时，仅对接口返回列表中唯一匹配的模型 ID 校正大小写。显示本次会话的连接验证状态，修改配置或测试失败后取消验证标记。密钥仍不回显。

所有基础研究改为用户配置的 Chat Completions 兼容 API：设置地址、密钥、模型，获取模型列表，测试连接；确认研究清单后自动执行逐条拆解和账号汇总。旧待处理报告可直接继续。

移除 AI 命令行执行器、复制指令按钮、外部对话交接脚本与技能依赖。保留本地采集、转写、视频/图文预览、评论采集、多选、来源隔离、失败重试和完整结果导出。

密钥仅保存本机，不回显或进入日志、资料包。接口请求不跟随重定向，远程地址仅 HTTPS；模型流式响应显示真实正文字数，空/截断/无效来源响应不算成功。用户主动调用其服务商 API 并承担相应费用，不内置任何商业凭据。

原报告和来源快照保留，旧等待状态迁移为 API 待开始研究。旧资料文件保留，但不再生成外部研究指令。升级前备份私有运行数据，升级后继续使用同一数据目录。

持续监控、机会评分、完整选题脚本、语言学习、发布复盘、剪辑编排和商业后台不公开。不存在通过隐藏开关封装核心商业代码。

发布前：隔离单元/接口测试、页面全流程、前端语法、白名单与哈希、干净解压启动。不得以模拟 API 验收冒充已验证所有真实服务商模型。仅在用户明确要求时公开发布，打包不等于发布。
